apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-app
  labels:
    app.kubernetes.io/name: api-app
    app.kubernetes.io/instance: api-app
    app.kubernetes.io/version: "1.14.2"
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: demo-app
    app.kubernetes.io/managed-by: kustomize
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: api-app
      app.kubernetes.io/instance: api-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: api-app
        app.kubernetes.io/instance: api-app
        app.kubernetes.io/version: "1.14.2"
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: demo-app
        app.kubernetes.io/managed-by: kustomize
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: api-app
              app.kubernetes.io/instance: api-app
      containers:
        - name: api-app
          image: kubernetes/media-api:1.0.0
          imagePullPolicy: IfNotPresent
          command: ["/app/api"]
          env:
            - name: PORT
              value: "8081"
            - name: BOOKS_API_HOST
              value: "books-service.books-app:8081" # <--- Ajout du namespace et du port
            - name: MOVIES_API_HOST
              value: "movies-service.movies-app:8081"
          ports:
            - name: http
              containerPort: 8081
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
